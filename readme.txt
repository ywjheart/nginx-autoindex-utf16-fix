This repo contains two fixes/enhancements

1. fixed nginx unicode support on windows
2. added native service mode support, no extra config required, just create it as service, it will detect and work.
This repo contains two fixes/enhancements

1. fixed nginx unicode support on windows
2. added native service mode support, no extra config required, just create it as service, it will work.
3. sigal also works in service mode(can send signal from any user session)